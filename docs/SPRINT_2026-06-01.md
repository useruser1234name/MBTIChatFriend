# 스프린트 보고서 — 2026-06-01 (보안/안정화)

3-에이전트 회의(보안·프로젝트리뷰·PM)에서 도출된 항목을 코드 검증 후 일괄 처리. 전 항목 회귀 테스트 통과.

## 검증 결과
- 서버 pytest: **110 passed** (기존 100 + 신규 10) — `tests/test_security_fixes.py`
- `app.main` import OK (서버 부팅 가능)
- Android `compileDebugKotlin` exit 0

---

## P0 (출시 차단) — 처리 완료

### P0-1. 비동기 DB 런타임 크래시
`postgres_async.py`의 `AsyncDatabase`가 `execute/fetchone/fetchall`만 정의했는데 6개 라우터(community, diary, notifications, referral, subscriptions, users)가 asyncpg식 `fetchrow/fetch/fetchval`을 호출 → 엔드포인트 진입 즉시 `AttributeError`. 추가로 `$1` 플레이스홀더가 psycopg3(`%s`)로 변환되지 않는 이중 버그.
- `_to_psycopg()` 변환기 추가(동기 `postgres.py`와 동일 규약), 전 메서드·raw 경로(`delete_conversation`, `check_daily_budget`)에 적용
- `fetchrow`/`fetch`/`fetchval` 별칭 추가
- `delete_conversation`이 실제 삭제 행수 반환(기존 하드코딩 `1` 버그 수정)

### P0-2. 결제 검증 우회 (무료 프리미엄)
`billing/verify-purchase`가 인증 없이 빈 토큰만 아니면 프리미엄 부여(Mock). RTDN webhook은 서명 검증 없음.
- `play_billing.py` 신규: Google Play Developer API 영수증 검증 + RTDN OIDC 토큰 검증. 라이브러리/설정 미구성 시 **production은 거부**, dev는 `BILLING_ALLOW_MOCK`일 때만 통과
- `verify-purchase`에 `require_auth_always` + uid 일치 + 영수증 검증 적용
- `rtdn-webhook`에 OIDC 토큰 검증 적용
- `subscription/upgrade`(mock)·`subscriptions/premium`도 동일 정책으로 강화

### P0-3. Android 콘텐츠 필터 비활성화
`ContentFilter.kt`의 금칙어/패턴 필터가 전부 주석 처리.
- 성적/혐오/스팸 필터 복원
- **위기 키워드(자살/자해/죽여)는 의도적으로 제외** — 클라이언트 차단 시 서버 Tier1 위기 개입 흐름이 막혀 오히려 위험하므로 서버로 통과시킴

---

## 보안 (High) — 처리 완료
- **IDOR**: `notifications`(전체), `data/consent`(GET/POST), `community`(create/delete/empathy/report/comment), `referral`(generate/redeem/stats) — 인증 + uid 일치 검증 추가
- **배치 엔드포인트 보호**: `referral/pending-notifications`·`send-d2-notifications`에 `require_internal_token`(X-Internal-Token) 가드 추가 (대량 FCM 남용 차단)
- **비용 남용**: `chat/starters`·`chat/greeting`에 `10/minute` 레이트리밋 추가
- **CORS**: 와일드카드 origin일 때 `allow_credentials` 자동 비활성
- **production 인증 강제**: `REQUIRE_AUTH=false`로 production 기동 시 `RuntimeError`로 중단
- **PII 로깅 제거**: `crisis_detected` 이벤트에서 `message_snippet`(원문 50자) 제거
- **BASE_URL**: buildType 분리(debug=로컬, release=HTTPS), gradle 프로퍼티 오버라이드

---

## 코드 품질 — 처리 완료
- `config.py` LLM 기본 모델 `gpt-4o`→`gpt-4.1`/`gpt-4.1-mini` (컨벤션 준수)
- `chat_service.py`: 미import된 `LLM_MODEL_SIMPLE` import 추가(잠복 NameError 해소), gpt-4o 폴백 교체, AI 응답 필터 이중 적용(C-2) 제거
- `data.py`·`ab_test.py`: 깨진 `INTERVAL '%s days'`(파라미터 미바인딩) → `make_interval(days => %s)`로 수정
- `letter.py`: 하드코딩 날짜(`2026-05-06`) → 동적 계산

---

## 2차 작업 — community 스키마 정합성 (실 DB 기준)

DDL(`postgres.py`)을 정본으로 community.py 쿼리를 전수 교정. 검증: 115 passed, app.main import OK, Android compileDebugKotlin exit 0.

발견·수정한 스키마 불일치 (모두 런타임 `column/table does not exist` 크래시였음):
- `p.mbti_type` → 실제 컬럼은 `mbti`. 클라이언트 `TrendingPostUi`는 `mbti_type` 키를 기대하므로 trending/public은 `p.mbti AS mbti_type`로 매핑, pinned/event(=`CommunityPost`)는 `p.mbti` + `anonymous_name` 포함.
- `post_empathies` 테이블 없음 → `community_empathies`로 (event-trending).
- `COUNT(DISTINCT e.id)` — `community_empathies`엔 `id` 컬럼 없음(복합 PK) → `COUNT(DISTINCT e.user_id)`.
- event-trending 날짜 `2027-04-10~05-01` 하드코딩(항상 빈 결과, C-4) → `now() - INTERVAL '31 days'` 연도 무관.

## 2차 작업 — PM 로드맵 착수

### 이벤트 로깅 (PM #1 — 최소 계측)
- `analytics_events.py`: 표준 이벤트 12종 + 허용 목록(`ALLOWED_CLIENT_EVENTS`).
- `routers/events.py`: `POST /api/v1/events/batch` (인증 필수, `60/minute`). user_id는 토큰에서 채워 위조 방지, 허용 목록 외 이벤트는 skip. `metric_events`(user_id 컬럼 이미 존재)에 적재.
- Android: `ChatApi.logEvents` + `EventBatchRequest/ClientEventDto/EventBatchResponse` DTO 추가.
- (남은 일: ViewModel에서 app_open/paywall_shown/purchase_complete 등 실제 호출 지점 연결.)

### 밤 일기 FREE 해금 (PM #2 — D7 훅)
- `subscription.py` PLAN_LIMITS: FREE `night_diary=True` + `night_diary_weekly_limit=3`, PREMIUM 무제한(-1).
- `SubscriptionManager.check_night_diary_limit()` 추가(주 3회, `diary_entries` room_id 기준 카운트, DB 미연결 시 낙관적 허용).
- `/diary/generate`에 게이트 연결(초과 시 402 + 업그레이드 안내).
- `SubscriptionStatusResponse.night_diary_weekly_limit` 필드 추가 + 두 응답 경로 반영.
- `scheduler.py`: 밤 21:00 KST `send_night_diary_push` 잡 추가(오늘자 diary_entries → 작성자 FCM).

### 부수 수정 (밤일기/스케줄러 토대)
- `scheduler.flush_empathy_notifications`: 존재하지 않는 `from postgres_async import _pool`(ImportError) → `get_async_db()` 래퍼로 교체(이제 실제 동작).
- `diary_entries` **DDL 중복 정의 충돌** 해소: 사용자 감정일기를 `user_diary_entries`로 분리(기존엔 IF NOT EXISTS로 무시되어 `/diary/entries` CRUD가 크래시). `routers/diary.py` 3개 쿼리 갱신.
- `fcm_tokens` 테이블 DDL 누락 → 추가(`firebase_service`가 참조).

---

## 미처리(후속 권장)
- **scheduler 잔여 잡**: `send_d3/d5_*`, `send_weekly_summary`, `send_gratitude_day_push`가 존재하지 않는 `messages`/`users` 테이블 + `_pool` import에 의존 → 비작동. 데이터 모델(대화 로그 테이블) 정비 필요한 별도 과제.
- **A-3** `lora_pipeline.py`(136K, 미import 데드코드) → `tools/`로 이동.
- **referral redeem 계약 불일치**: Android는 `@Body` POST, 서버는 `code` 쿼리 파라미터 — 기존부터 불일치(이번 보안 수정과 무관). 통합 필요.
- **PM 로드맵 잔여**: 이벤트 ViewModel 연결, Google Play Billing 실연동, "별명 짓기" D1 훅.
