package com.example.mbtichatfriend.ui.theme

/**
 * 공용 애니메이션 duration 토큰 (U1, 밀리초).
 *
 * 정의만 제공 — 기존 tween(...)/animationSpec 리터럴의 일괄 치환은 이번 항목의
 * 범위가 아니며, U2 이후 각 항목이 실제로 손대는 파일에서만 점진적으로 적용한다.
 */
object MotionDurations {
    const val Short = 300
    const val Medium = 400
}
