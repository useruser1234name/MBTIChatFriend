# OpenAI API Key (필수)
OPENAI_API_KEY=your-api-key-here

# 서버 설정
HOST=0.0.0.0
PORT=8000

# Firebase (선택)
FIREBASE_CREDENTIALS_PATH=

# CORS 허용 origin (쉼표 구분, 기본: * 전체 허용)
# 프로덕션: CORS_ORIGINS=https://yourdomain.com
CORS_ORIGINS=*

# 환경 (development / production)
ENVIRONMENT=development
